name: speedometer
description: A little widget that given an Observable gives you an updated SpeedOMeter.
version: 1.5.0
homepage: https://github.com/mvitolo/speedometer

environment:
  sdk: '>=2.18.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: '>=0.25.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
