name: example
description: A SpeedOMeter example for flutter

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  speedometer:
   path: ../

flutter:
  uses-material-design: true
